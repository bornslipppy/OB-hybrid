# Test fixtures with intentionally broken/malformed files
test/fixtures/**

# Contributor Covenant (external standard)
CODE_OF_CONDUCT.md

# BMAD runtime folders (user-specific, not in repo)
_bmad/
_bmad*/

# IDE integration folders (user-specific, not in repo)
.junie/

# PoC harness — Python virtualenv (machine-generated, never formatted)
poc-eval-harness/.venv/

# PoC harness — eval run outputs (gitignored; local-only)
poc-eval-harness/campaigns/
poc-eval-harness/runs/

# Guesty working documents (pre-existing, not framework artifacts)
docs/inbox-v2-documentation/
